<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CryptoCurrency" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_dark</item>
        <item name="colorOnPrimary">@color/color_white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_accent</item>
        <item name="colorSecondaryVariant">@color/color_accent</item>
        <item name="colorOnSecondary">@color/color_white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="GradientTheme" parent="Theme.CryptoCurrency">
        <item name="android:windowBackground">@drawable/gradient</item>
    </style>
    <style name="MainScreenTheme" parent="Theme.CryptoCurrency">
        <item name="android:background">@color/color_transparent</item>
        <item name="android:textColor">@color/color_white</item>
        <item name="android:iconTint" tools:targetApi="o">@color/color_white</item>
        <item name="android:backgroundTint">@color/color_white</item>
    </style>
</resources>
